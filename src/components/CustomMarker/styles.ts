import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  pinSize: {
    width: 20,
    height: 31,
  },
  heart: {
    position: 'absolute',
    left: 3,
    top: 3,
  },
});

export default styles;
